import { EnumValuePipe } from './enum-value.pipe';

describe('EnumValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumValuePipe();
    expect(pipe).toBeTruthy();
  });
});
